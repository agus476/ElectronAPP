function search() {
    // Obtener el valor del input
    userInput = document.getElementById('searchInput').value;

    // Mostrar el valor en la consola (solo para verificar)
    console.log('Input:', userInput);

    // Puedes hacer lo que quieras con el valor aquí
    // Por ejemplo, puedes usarlo para buscar en tu base de datos, actualizar la interfaz de usuario, etc.
}